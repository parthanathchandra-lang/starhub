name: StarHub V12 Auto Sync + Auto Renewal
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
  pages: write
  id-token: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Auto Correction + Auto Renewal
        run: |
          cp index.html 404.html
          touch.nojekyll
          ls -la
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder:.
          branch: gh-pages
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
      - name: Deploy to Surge
        run: |
          npm i -g surge
          surge./ --domain starhub-eta.surge.sh --token ${{ secrets.SURGE_TOKEN }}
